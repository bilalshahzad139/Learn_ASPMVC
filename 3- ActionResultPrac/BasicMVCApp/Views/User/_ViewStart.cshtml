@{
    /*
     * Here we are setting a Layout, by default this 'Layout' will be used as master laytout
     * If any page will set 'Layout' property to different value, that will be used
     * Actions can also provide 'Layout' value through code
     */
    
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}